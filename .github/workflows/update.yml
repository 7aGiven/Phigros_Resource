on:
  workflow_dispatch:
    inputs:
      url:
        description: "download url"
        required: true
        type: string
jobs:
  my_job:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - run: pip install UnityPy

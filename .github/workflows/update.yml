on:
  workflow_dispatch:
    inputs:
      url:
        description: "download url"
        required: true
        type: string
jobs:
  my_job:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - run: |
          git add .
          git commit -m "update resource"
          git push
